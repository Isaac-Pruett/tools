{ pkgs }: {
  tmux     = pkgs.tmux;
  kitty    = pkgs.kitty;
  zsh      = pkgs.zsh;
  starship = pkgs.starship;
  fzf      = pkgs.fzf;
  ripgrep  = pkgs.ripgrep;
  fd       = pkgs.fd;
  bat      = pkgs.bat;
  eza      = pkgs.eza;
  zoxide   = pkgs.zoxide;
  atuin    = pkgs.atuin;
  lazygit  = pkgs.lazygit;
  delta    = pkgs.delta;
}
